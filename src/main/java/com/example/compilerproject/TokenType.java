package com.example.compilerproject;

// TokenType represents the various and different types of tokens that the compiler can execute
public enum TokenType {
    RESERVED,
    OPERATOR,

    Punctuation,

    IDENTIFIER,

    INTEGER,

    REAL,
    UNKNOWN
}
